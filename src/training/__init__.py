# src.training — Training loop and utilities.
# Import from submodules directly:
#   from src.training.train import train_epoch, validate_epoch, train_model
#   from src.training.utils import set_seed, get_device, ...
